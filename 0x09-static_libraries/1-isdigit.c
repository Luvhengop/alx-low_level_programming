#include "main.h"

/**
 * _isdigit - function declaration
 * Description: test if digit
 * @c: value to check
 * Return: 1 for true, 0 for otherwwise
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
